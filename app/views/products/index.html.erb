<h1>Add Product</h1>

<%= form_tag("/", method: "post") do %>
  <%= label_tag(:product, "Add Product:") %>
  <%= text_field_tag(:product) %>
  <%= submit_tag("add to cart")  %>
<% end %>

<h1>Cart</h1>

<ul>
  <% cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>
